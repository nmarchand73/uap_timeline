# Vidéo documentaire (local uniquement)

Le documentaire **The Age of Disclosure** est toujours servi depuis un fichier local. Aucun stream externe (YouTube, etc.) n’est utilisé.

- **Fichier vidéo** : `The.Age.Of.Disclosure.2025.mkv` — à placer dans ce dossier (`public/video/`).
- **Sous-titres** : `The.Age.Of.Disclosure.2025.vtt` (générés par `npm run srt-to-vtt`).

La page `/documentary/` référence uniquement ce fichier MKV en local.
